export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
}
export const DEFAULT_ORDER = Order.ASC

export enum Sort {
    DISCOVERED = 'DISCOVERED',
    NAME = 'NAME',
}
export const DEFAULT_SORT = Sort.DISCOVERED
